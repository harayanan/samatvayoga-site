<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Warm cream background circle -->
  <circle cx="32" cy="32" r="31" fill="#fef7ed"/>

  <!-- Om symbol (ॐ) in saffron -->
  <text
    x="32"
    y="44"
    text-anchor="middle"
    font-family="serif"
    font-size="40"
    fill="#df600b"
  >&#x0950;</text>

  <!-- Subtle ring border -->
  <circle cx="32" cy="32" r="30.5" fill="none" stroke="#df600b" stroke-width="0.8" opacity="0.3"/>
</svg>
